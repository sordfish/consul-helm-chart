apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "consul.fullname" . }}-test-connection"
  labels:
    {{- include "consul.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "consul.fullname" . }}:8500']
  restartPolicy: Never
